import React, { useState } from "react";
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  StatusBar,
  SafeAreaView,
  ScrollView,
  Image,
} from "react-native";
import { FoodCard } from "../components";

const HomeScreen = () => {
  return <FoodCard />;
};

export default HomeScreen;
